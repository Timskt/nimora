{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet-window-dragging",
  "description": "Allows only the first-party pet window to begin a native desktop drag",
  "windows": ["pet"],
  "permissions": ["core:window:allow-start-dragging"]
}
