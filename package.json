{
  "name": "nimora-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=22.22.0",
    "pnpm": ">=11.1.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "build:sidecars": "node scripts/build-sidecars.mjs",
    "check": "pnpm check:architecture && pnpm lint && pnpm typecheck && pnpm test",
    "check:architecture": "node scripts/check-architecture-boundaries.mjs",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:rust": "cargo test --workspace",
    "lint:rust": "cargo clippy --workspace --all-targets -- -D warnings",
    "format:rust": "cargo fmt --all --check"
  }
}
